# Lattice

Phase 2 prototype scaffold for a Semantic IR pipeline. The canonical data model lives in `src/schema.ts`; no pipeline stages (segmentation, extraction, or graph assembly) are implemented yet.
